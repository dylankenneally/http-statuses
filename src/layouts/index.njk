{% extends "base.njk" %}

{% block content %}
<section>
  <h1 class="banner-title">{{ strings.siteName }}</h1>
  <p>{{ strings.siteDescription }}</p>
  {{ content | safe }}
</section>

<nav class="main-nav">
  <ul>
    <li><a href="#informational">Informational</a></li>
    <li><a href="#successful">Successful</a></li>
    <li><a href="#redirection">Redirection</a></li>
    <li><a href="#client-error">Client error</a></li>
    <li><a href="#server-error">Server error</a></li>
  </ul>
</nav>

<h2 id="informational">Informational</h2>
<div class="card-layout">
  {% for item in collections.informational %}
  <a href={{item.url | url }} class="image-background_container card-layout_item hover-highlight">
    <div class="material-symbols-outlined image-background_image informational">{% include "info_bold.svg" %}</div>
    <div class="image-background_content ellipsis-content">
      <h3>{{ item.data.title }}</h3>
      {{- item.data.overview | markdown | safe -}}
    </div>
  </a>
  {% endfor %}
</div>

<h2 id="successful">Successful</h2>
<div class="card-layout">
  {% for item in collections.successful %}
  <a href={{item.url | url }} class="image-background_container card-layout_item hover-highlight">
    <div class="material-symbols-outlined image-background_image successful">{% include "success_bold.svg" %}</div>
    <div class="image-background_content ellipsis-content">
      <h3>{{ item.data.title }}</h3>
      {{- item.data.overview | markdown | safe -}}
    </div>
  </a>
  {% endfor %}
</div>

<h2 id="redirection">Redirection</h2>
<div class="card-layout">
  {% for item in collections.redirection %}
  <a href={{item.url | url }} class="image-background_container card-layout_item hover-highlight">
    <div class="material-symbols-outlined image-background_image redirect">{% include "redirect_bold.svg" %}</div>
    <div class="image-background_content ellipsis-content">
      <h3>{{ item.data.title }}</h3>
      {{- item.data.overview | markdown | safe -}}
    </div>
  </a>
  {% endfor %}
</div>

<h2 id="client-error">Client error</h2>
<div class="card-layout">
  {% for item in collections['client error'] %}
  <a href={{item.url | url }} class="image-background_container card-layout_item hover-highlight">
    <div class="material-symbols-outlined image-background_image client-error">{% include "client_error_bold.svg" %}</div>
    <div class="image-background_content ellipsis-content">
      <h3>{{ item.data.title }}</h3>
      {{- item.data.overview | markdown | safe -}}
    </div>
  </a>
  {% endfor %}
</div>

<h2 id="server-error">Server error</h2>
<div class="card-layout">
  {% for item in collections['server error'] %}
  <a href={{item.url | url }} class="image-background_container card-layout_item hover-highlight">
    <div class="material-symbols-outlined image-background_image server-error">{% include "server_error_bold.svg" %}</div>
    <div class="image-background_content ellipsis-content">
      <h3>{{ item.data.title }}</h3>
      {{- item.data.overview | markdown | safe -}}
    </div>
  </a>
  {% endfor %}
</div>

<p class="main-footer">© {{ strings.authorName }} 2024. <a href="mailto:{{ strings.authorEmail }}">Contact</a></p>

{% endblock %}
